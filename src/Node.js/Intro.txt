* Node.js® is a JavaScript runtime built on Chrome's V8 JavaScript engine.It is an architecture framework for building out web applications

* "package.json" file contains all of the information which is required for your application to work like dependencies

* "npm install" bash command will install additional packages/libraries that is required, need to bash in the package.json directory.