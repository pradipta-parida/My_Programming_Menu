"use strict"; // Treat all JS code as newer version.By default, it's used but its optional to mention it

// alert(3+3) // we are using nodejs, not browser

let age = 34.88;

/*
Data Types in JS;
Number => 
bigint =>
string => ""
boolean => true/false
null => standalone value
undefined => value not assigned to a variable
symbol => unique
*/

// Displays type of any value or variable in console
console.log(typeof ("pradipta"));
console.log(typeof (age));
console.log(typeof (null)); // object
console.log(typeof (undefined)); // undefined
